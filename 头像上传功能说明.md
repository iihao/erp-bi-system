# 头像上传功能实现说明

## 功能概述

完善了个人设置页面的上传头像功能，支持以下特性：
- ✅ 点击或拖拽上传图片
- ✅ 支持 JPG、PNG、GIF、WebP 格式
- ✅ 文件大小限制 2MB
- ✅ 图片预览和裁剪
- ✅ 缩放和旋转调整
- ✅ 圆形头像预览
- ✅ 上传进度显示
- ✅ 自动保存到数据库

## 文件变更

### 后端 (`/backend`)

1. **api/profile.py**
   - 新增 `POST /api/profile/avatar` - 上传头像接口
   - 新增 `DELETE /api/profile/avatar` - 删除头像接口
   - 支持文件类型验证、大小验证
   - 自动保存到 `uploads/avatars` 目录
   - 同时更新 `users` 和 `user_profiles` 表

2. **main.py**
   - 添加 `/uploads` 静态文件挂载
   - 自动创建 uploads 目录

### 前端 (`/frontend`)

1. **src/components/AvatarUploadDialog.vue** (新建)
   - 头像上传对话框组件
   - 支持拖拽上传
   - 图片预览和裁剪功能
   - 缩放 (0.5x - 3x) 和旋转 (±90°) 控制
   - 圆形预览效果
   - 上传进度显示

2. **src/views/Profile.vue**
   - 集成 AvatarUploadDialog 组件
   - 移除简单的头像 URL 输入框
   - 添加头像上传成功回调处理

3. **src/api/index.js**
   - 添加 `profile.uploadAvatar()` 方法
   - 添加 `profile.deleteAvatar()` 方法
   - 支持上传进度回调

## 使用方法

### 用户操作流程

1. 进入个人主页
2. 点击头像右下角的编辑按钮（铅笔图标）
3. 在弹出的对话框中：
   - 点击区域或拖拽图片上传
   - 调整缩放和旋转
   - 预览裁剪效果
   - 点击"确认上传"
4. 等待上传完成，头像自动更新

### API 调用示例

```javascript
// 上传头像
const file = fileInput.files[0]
const result = await api.profile.uploadAvatar(file, (percent) => {
  console.log(`上传进度：${percent}%`)
})

if (result.success) {
  console.log('头像 URL:', result.data.avatar_url)
}

// 删除头像
const result = await api.profile.deleteAvatar()
```

## 技术细节

### 后端处理流程

1. 接收上传的文件
2. 验证文件类型（JPEG/PNG/GIF/WebP）
3. 验证文件大小（≤2MB）
4. 生成唯一文件名（UUID）
5. 保存到 `backend/uploads/avatars/` 目录
6. 更新数据库中的头像 URL
7. 返回头像访问 URL

### 前端处理流程

1. 用户选择或拖拽文件
2. 创建临时 URL 预览图片
3. 使用 Canvas 进行裁剪和旋转
4. 将裁剪后的图片转换为 Blob
5. 通过 FormData 上传到后端
6. 显示上传进度
7. 上传成功后更新头像显示

### 数据库变更

头像 URL 同时存储在两个表中：
- `users.avatar_url` - 用户基本信息
- `user_profiles.avatar_url` - 用户扩展信息

这样设计是为了兼容不同的查询场景。

## 注意事项

1. **生产环境配置**
   - 确保 `uploads` 目录有写权限
   - 配置 Web 服务器（Nginx/Apache）代理 `/uploads` 路径
   - 考虑使用 CDN 存储头像图片

2. **安全性**
   - 已实现文件类型验证
   - 已实现文件大小限制
   - 使用 UUID 生成文件名防止冲突
   - 建议添加图片内容验证（防止恶意文件）

3. **性能优化建议**
   - 添加图片压缩（当前使用 0.9 质量）
   - 考虑生成多种尺寸（缩略图、中图、大图）
   - 使用 CDN 加速头像访问
   - 添加浏览器缓存策略

## 测试检查清单

- [ ] 上传 JPG 格式图片
- [ ] 上传 PNG 格式图片（含透明背景）
- [ ] 上传 GIF 格式图片
- [ ] 上传 WebP 格式图片
- [ ] 拖拽上传功能
- [ ] 文件过大提示（>2MB）
- [ ] 格式错误提示
- [ ] 缩放功能正常
- [ ] 旋转功能正常（左旋/右旋）
- [ ] 圆形预览效果
- [ ] 上传进度显示
- [ ] 上传成功后头像立即更新
- [ ] 多个用户上传头像不冲突

## 后续优化建议

1. **图片裁剪优化**
   - 使用专业的裁剪库（如 cropperjs）
   - 支持自由裁剪（不只是圆形）
   - 添加裁剪框拖拽调整

2. **上传优化**
   - 添加上传失败重试
   - 支持取消上传
   - 添加图片压缩（前端）

3. **头像管理**
   - 头像历史记录
   - 恢复默认头像
   - 头像选择器（预设头像）

4. **性能优化**
   - 生成多尺寸头像
   - 懒加载头像
   - CDN 集成
