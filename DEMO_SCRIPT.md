# 🎓 毕业答辩演示脚本

> AI数据融合平台 | 基于 ERP 系统的商业智能报表的设计与实现 | 5 分钟演示

---

## 📋 演示前准备（2 分钟）

### 1. 启动所有服务
```bash
cd /Users/huangqiang/.openclaw/workspace/erp-bi-system
docker-compose up -d
```

### 2. 等待服务就绪
- MySQL: 约 30 秒
- Metabase: 约 2-3 分钟（首次启动）

### 3. 初始化数据库
```bash
mysql -h 127.0.0.1 -P 3306 -u root -proot123 erp_bi < init_scripts/erp_init.sql
mysql -h 127.0.0.1 -P 3306 -u root -proot123 erp_bi < metabase/init_views.sql
```

### 4. 启动前端和后端
```bash
# 终端 1 - 后端
cd backend
uvicorn main:app --reload

# 终端 2 - 前端
cd frontend
npm run dev
```

### 5. 打开浏览器标签页
- **Tab 1**: http://localhost:3000 (前端管理后台)
- **Tab 2**: http://localhost:3001 (Metabase BI)
- **Tab 3**: http://localhost:8000/docs (API 文档)

---

## 🎬 正式演示流程（5 分钟）

### 第 1 分钟：系统架构介绍

**操作**：打开 README.md 或 PPT 架构图

**话术**：
> "各位老师好，我的毕业设计题目是《基于 ERP 系统的商业智能报表的设计与实现》。
> 
> 系统采用经典的数仓分层架构，分为四层：
> - **ODS 层**：原始数据层，直接从 ERP 业务库抽取数据
> - **DWD 层**：明细数据层，进行数据清洗和标准化
> - **DWS 层**：汇总数据层，轻度聚合提升查询性能
> - **ADS 层**：应用数据层，直接支撑报表展示
> 
> 技术栈包括：MySQL 业务数据库、FastAPI 后端、Vue3 前端、Metabase BI 工具，
> 以及创新功能——基于百炼大模型的 AI 智能问数系统。"

---

### 第 2 分钟：数据流转演示

**操作**：切换到前端管理后台 → 数据预览页面

**演示步骤**：
1. 打开 http://localhost:3000/data
2. 依次展示 5 张业务表：
   - 产品表 (products)
   - 客户表 (customers)
   - 销售订单表 (sales_orders)
   - 订单明细表 (sales_order_items)
   - 供应商表 (suppliers)

**话术**：
> "这是系统的业务数据层，包含 5 张核心表，模拟了真实的 ERP 业务场景。
> 
> 现在展示的是经过 ETL 处理后的数据，从原始业务库抽取到 ODS 层，
> 经过 DWD 清洗、DWS 聚合，最终在 ADS 层形成报表指标。
> 
> 整个 ETL 流程可以通过任务管理页面进行调度和监控。"

---

### 第 3 分钟：BI 报表展示

**操作**：切换到 Metabase BI 页面

**演示步骤**：
1. 打开 http://localhost:3001
2. 登录（如已设置）
3. 打开"ERP 销售分析大屏"仪表板
4. 依次介绍各个图表

**话术**：
> "这是 Metabase BI 报表系统，用于可视化展示 ADS 层的报表数据。
> 
> 仪表板包含：
> - **KPI 卡片**：总销售额、总订单数、客户总数等核心指标
> - **月度销售趋势**：近 12 个月的销售变化
> - **产品销量排行**：Top10 热销产品
> - **品类销售占比**：各产品类别的销售分布
> - **客户订单分析**：客户消费明细
> 
> 业务人员可以通过拖拽方式自助创建报表，无需编写 SQL。"

---

### 第 4 分钟：AI 智能问数 ⭐ 创新功能

**操作**：切换到前端 → AI 问数页面

**演示步骤**：
1. 打开 http://localhost:3000/ai-query
2. 演示 3 个自然语言查询：

**查询 1**：
```
问题：上个月销售额最高的产品是什么？
```

**查询 2**：
```
问题：各品类的销售占比是多少？
```

**查询 3**：
```
问题：客户张三的所有订单
```

**话术**：
> "这是本系统的创新功能——AI 智能问数。
> 
> 用户只需用自然语言提问，系统会自动生成 SQL 并执行查询。
> 背后使用的是阿里云百炼大模型 Qwen3.5-Plus。
> 
> 例如，输入'上个月销售额最高的产品是什么？'
> 系统会生成对应的 SQL 查询语句，并返回结果。
> 
> 这大大降低了数据查询的门槛，业务人员无需学习 SQL 即可获取数据。"

---

### 第 5 分钟：后台管理 + 总结

**操作**：切换到 ETL 任务管理页面

**演示步骤**：
1. 打开 http://localhost:3000/etl
2. 点击"运行全部任务"按钮
3. 展示任务执行日志

**话术**：
> "最后是后台管理模块，包含：
> - **ETL 任务调度**：定时执行数据抽取、清洗、聚合任务
> - **权限管理**：基于 RBAC 的用户权限控制
> - **系统监控**：服务健康检查、日志查看
> 
> 总结一下，本系统实现了：
> 1. ✅ 完整的数仓分层架构（ODS/DWD/DWS/ADS）
> 2. ✅ ETL 数据流水线
> 3. ✅ BI 可视化报表
> 4. ✅ AI 智能问数创新功能
> 5. ✅ 后台管理模块
> 
> 感谢各位老师聆听，请批评指正！"

---

## 📝 答辩准备清单

### 技术准备
- [ ] Docker 服务全部启动
- [ ] 数据库初始化完成
- [ ] 前端后端运行正常
- [ ] Metabase 仪表板配置好
- [ ] 浏览器标签页预加载

### 文档准备
- [ ] README.md 打开
- [ ] PROJECT_STATUS.md 准备好
- [ ] 架构图 PPT（如有）
- [ ] 演示脚本打印版

### 备用方案
- [ ] 录屏视频（防止现场演示出问题）
- [ ] 截图文档
- [ ] 本地数据库备份

---

## ❓ 可能的问题准备

### Q1: 为什么选择数仓分层架构？
**A**: 分层设计实现数据解耦。ODS 保持原始数据可追溯，DWD 统一数据质量，DWS 提升查询性能，ADS 直接支撑业务应用。这种架构在企业级数据平台中广泛应用。

### Q2: AI 问数的准确率如何？
**A**: 基于百炼 Qwen 大模型，在明确的问题场景下准确率可达 85%+。通过提供表结构上下文和 few-shot 提示优化，可以进一步提升生成质量。对于复杂查询，系统会生成 SQL 供用户确认后再执行。

### Q3: 系统如何保证数据一致性？
**A**: ETL 任务采用事务处理，失败自动回滚。定时任务调度确保数据更新频率。关键指标设置数据质量校验规则，如销售额不能为负、订单日期不能为空等。

### Q4: 和传统 BI 系统相比有什么优势？
**A**: 传统 BI 主要依赖预定义报表，而本系统引入了 AI 智能问数，支持即席查询。业务人员无需 IT 支持即可获取数据，大大提升了数据分析的灵活性和效率。

### Q5: 系统性能如何？
**A**: 对于演示规模的数据（几十条记录），查询响应时间在秒级。生产环境可通过以下方式优化：DWS 层预聚合、Redis 缓存热点数据、数据库索引优化、分页查询等。

---

## 🎯 演示成功关键

1. **提前演练**：至少完整演练 3 遍，控制时间在 5 分钟内
2. **环境稳定**：确保所有服务正常运行，准备备用录屏
3. **节奏把控**：每部分 1 分钟，不要在某一部分停留过久
4. **突出亮点**：AI 智能问数是创新点，重点演示
5. **自信表达**：语速适中，声音清晰，与评委有眼神交流

---

**祝答辩顺利！🎓**
